# Foundation bundle v0.2 section schema
# Canonical machine-readable instance for foundation-doc-type section schemas + shape-extension metadata.
# v0.1 → v0.2 transition at the foundation-bundle-v0.2 release 2026-05-20: test_cases entry rewritten
# from 3-section markdown-agents-locked structure (test_triggers / test_coverage_requirements /
# agent_specific_tests) to 5-section shape-neutral structure (requirements_anchor / acceptance_criteria
# / test_pyramid / validation_method / markdown_agent_validation_matrix). Pre-v1 stabilization clause
# applies per the foundation-versioning policy.
#
# Schema discipline:
# - This YAML is the canonical machine-readable instance for section schemas.
# - The canonical human-readable prose specification lives in the build-side foundation versioning
#   documentation (not distributed in this directory).
# - The build-side render-contract validator reads this YAML; it does NOT embed schema constants.
# - Drift between this YAML and the canonical prose specification is detected via
#   governance_prose_section_1_4_hash (bump explicitly when the canonical prose changes).
#
# Field semantics:
#   id: stable section ID (string; never changes across schema revisions)
#   title: operator-visible heading text (string)
#   required: section presence required for valid bundle (bool; preserves v0 semantics)
#   applies_to: [all]                                  XOR with applies_to_shapes
#   applies_to_shapes: [<shape-list>]                  XOR with applies_to; values must be in shape_vocabulary
#   render_modes: [complete | foundation_only | both]  list enum
#   operator_visible: bool (default true)
#   population_status: populated | deferred | blocked_pending_replacement (default populated)
#   undefer_trigger: prose (required when population_status != populated)
#   source: template | runtime | schema_only           # distinguishes template-backed vs runtime-generated vs forward-declared sections

section_schema_version: v0.2
schema_authority: foundation-versioning-section-schema-v0.2
governance_prose_section_1_4_hash: b67aadb1f1a47f6d64e7b25cbcd3f812a6e01e4a  # bumped at the foundation-bundle-v0.2 release via `validate_foundation_render_contract.py regenerate-prose-hash --rationale ...`
generated_by_release: foundation-bundle-v0.2
generated_date: 2026-05-20
shape_vocabulary:
  - markdown-agents-on-claude-code
  - python-service
  - node-ui
  - hosted-cloud
  - mixed

section_schema:

  # ============================================================
  # vision.md — fully shape-neutral foundation doc
  # ============================================================
  vision:
    sections:
      - id: purpose
        title: Purpose
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template
      - id: goals_objectives_priorities
        title: Goals, Objectives, and Priorities
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template
      - id: audience_and_outputs
        title: Audience and Outputs
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template
      - id: scope_boundary
        title: Scope Boundary
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template
      - id: constraints
        title: Constraints
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template
      - id: success_criteria
        title: Success Criteria
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template

  # ============================================================
  # approach.md — shape-specific Agent Roster; otherwise neutral
  # ============================================================
  approach:
    sections:
      - id: solution_brief
        title: Solution Brief
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template
      - id: agent_roster
        title: Agent Roster
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: component_roster
        title: Component Roster
        required: false
        applies_to_shapes: [python-service, node-ui, hosted-cloud, mixed]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "First Stage 2.X slice that adds non-markdown-agent shape generation interview steps; component roster content authored when shape support ships"
        source: schema_only

  # ============================================================
  # technical_architecture.md — heavily shape-specific
  # ============================================================
  technical_architecture:
    sections:
      - id: orchestration_model
        title: Orchestration Model
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: agent_roster
        title: Agent Roster
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: permission_boundaries
        title: Permission Boundaries
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: task_completion_checklists
        title: Task Completion Checklists
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: integrations
        title: Integrations
        required: false
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: scale_tier
        title: Scale Tier
        required: false
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: regulatory_compliance_gaps_foundation_only
        title: "Regulatory & compliance gaps (foundation-only mode)"
        required: false
        applies_to: [all]
        render_modes: [foundation_only]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Populated when DOCUMENT-path stop conditions are documented during the stop-condition reevaluate loop."
      - id: deployment_topology
        title: Deployment Topology
        required: false
        applies_to_shapes: [python-service, node-ui, hosted-cloud, mixed]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "First Stage 2.X slice that adds deployment-topology interview content for the relevant non-markdown shape"
        source: schema_only
      - id: data_model
        title: Data Model
        required: false
        applies_to_shapes: [python-service, node-ui, hosted-cloud, mixed]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "First Stage 2.X slice that adds data-model interview content for the relevant non-markdown shape"
        source: schema_only

  # ============================================================
  # execution_plan.md — shape-specific Build Phases / HITL Map / Work Plans
  # ============================================================
  execution_plan:
    sections:
      - id: mvp_definition
        title: MVP Definition
        required: true
        applies_to: [all]
        render_modes: [complete, foundation_only]
        operator_visible: true
        population_status: populated
        source: template
      - id: build_phases
        title: Build Phases
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: execution_sequence
        title: Execution Sequence
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: human_in_the_loop_map
        title: Human-in-the-Loop Map
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: work_tracking
        title: Work Tracking
        required: false
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: deployment_pipeline
        title: Deployment Pipeline
        required: false
        applies_to_shapes: [python-service, node-ui, hosted-cloud, mixed]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "First Stage 2.X slice that adds deployment-pipeline interview content for the relevant non-markdown shape"
        source: schema_only

  # ============================================================
  # test_cases.md — shape-neutral universal 4-section + markdown shape extension
  # Replaced at foundation-bundle-v0.2 release 2026-05-20 (v0.1 → v0.2)
  # ============================================================
  test_cases:
    coverage_by_shape:
      markdown-agents-on-claude-code: populated
      python-service: populated
      node-ui: populated
      hosted-cloud: populated
      mixed: populated
    foundation_only_render: SKIP  # foundation-only mode skips test_cases rendering (preserved per the prior relevant slice decision on foundation-only bundle composition)
    non_markdown_blocked_render_gate: "Render-contract validator FAILs full non-markdown bundle generation if ANY shape has coverage_by_shape: blocked_pending_replacement; gate is preserved in validator code per AT-11 and fires for any future shape added without populating coverage. At v0.2 all 5 known shapes are populated so the gate does not fire by default; the gate is fail-closed structural defense, not an active block."
    sections:
      - id: requirements_anchor
        title: Requirements Anchor
        required: false
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "Wizard interview supports PRD authoring and prd.md is populated for the operator's shape (currently deferred per prd entry source: schema_only at v0.x)"
        source: schema_only
        notes: "Operator's reference back to PRD functional/non-functional requirements + JTBD entries. Section is deferred at v0.2 because prd.md is source: schema_only (also deferred); requirements_anchor un-defers and becomes template-backed when prd authoring lands. Matches existing pattern: deferred + schema_only = schema-declaration only; NOT rendered in template."
      - id: acceptance_criteria
        title: Acceptance Criteria
        required: true
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Universal acceptance criteria across all shapes. Houses universal-concern test categories from prior 29-category disposition (E error handling concepts; L HITL universal; V validation discipline; C security/secrets; P PII redaction; SC scale monitoring; ID idempotency for external integrations; A-1..A-5 + A-30 + A-37..A-40 permission/atomicity/checkpoint/retry/idempotency; T completion + document-currency + vision-alignment; F-2..F-5 + F-9 + F-19 universal cost controls; Q-9..Q-23 security audit triggers + thresholds + quarantine; N alert routing + severity; B blast radius + scope declaration)."
      - id: test_pyramid
        title: Test Pyramid
        required: false
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Layer distribution (unit / integration / e2e or shape-equivalent) chosen by operator per shape + risk profile."
      - id: validation_method
        title: Validation Method
        required: true  # tightened per the-relevant-slice-decision review: every bundle should have a validation method even if minimal
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Concrete validation tooling per layer; cost-aware; chosen per deployment + observability surface. Minimum: how each acceptance criterion is verified (manual / scripted / automated)."
      - id: markdown_agent_validation_matrix
        title: Markdown Agent Validation Matrix
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]  # NOT applied to mixed shape; mixed shape requires component-level metadata at future slice
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Houses markdown-agents-on-claude-code-shape-specific test categories from prior disposition: H handoff/coordination, A-6..A-52 agent identity/permissions (excluding universal sub-concepts), M MCP resilience, S session startup, D document update mechanism, AC auto-correct, G git/version control, R rationale propagation, O operational self-analysis, REV model-as-reviewer reviews, FI future items register, SCE session close enforcement, VS voice/style; plus shape-specific subsets from mixed categories (Q-1..Q-8 QA-agent specific; EN-18..EN-19 manual.md; MT-3..MT-11 agent prompts + scripts tier names; F-6..F-8 + F-10..F-26 markdown-specific cost/budget impl; A-row-level markdown-specific subset)."
      - id: test_maintenance
        title: Test maintenance
        required: false
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Universal narrative on how the test inventory is maintained over the system's life (wizard-shipped updates, operator-discovered scenarios, failing-test review). Shape-neutral; renders in complete mode only (test_cases is foundation_only SKIP)."

  # ============================================================
  # audit_framework.md — Security + Drift universal; Log + Architecture shape-specific
  # ============================================================
  audit_framework:
    sections:
      - id: security_audits
        title: Security Audits
        required: true
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: log_audits
        title: Log Audits
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Uses /logs/ path convention specific to markdown-agents-on-claude-code shape"
      - id: architecture_audits
        title: Architecture Audits
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Phase-gate review references autonomy levels — markdown-agents-on-claude-code-specific"
      - id: drift_audits
        title: Drift Audits
        required: true
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
      - id: rules_library
        title: Rules library
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Describes /quality/rules_library.md, QA-agent writes, and agent reads — markdown-agents-on-claude-code agent infrastructure."
      - id: system_lifecycle
        title: System lifecycle
        required: false
        applies_to_shapes: [markdown-agents-on-claude-code]
        render_modes: [complete]
        operator_visible: true
        population_status: populated
        source: template
        notes: "Maintenance + upgrade lifecycle writes referencing markdown-agents-on-claude-code filesystem conventions (/archive/logs/, work_archive, session-bootstrap state)."

  # ============================================================
  # prd.md — required public API filename but not currently template-generated
  # ============================================================
  prd:
    sections:
      - id: vision_link
        title: Vision Link
        required: true
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "Wizard interview supports PRD authoring (post-Stage-2 scope; deferred)"
        source: schema_only
      - id: persona_jtbd
        title: Persona / JTBD
        required: true
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "Wizard interview supports PRD authoring (post-Stage-2 scope; deferred)"
        source: schema_only
      - id: functional_requirements
        title: Functional Requirements
        required: true
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "Wizard interview supports PRD authoring (post-Stage-2 scope; deferred)"
        source: schema_only
      - id: non_functional_requirements
        title: Non-Functional Requirements
        required: true
        applies_to: [all]
        render_modes: [complete]
        operator_visible: true
        population_status: deferred
        undefer_trigger: "Wizard interview supports PRD authoring (post-Stage-2 scope; deferred)"
        source: schema_only
